IP_CONST = '192.168.137.112'
MAC_CONST = '4344A6084119'

class database:
    def update(aggregate[]):
        #get data
    def get_student_id(device):
        #sql query (like jake) to mac addr table
        #get query results
        #extract ID from results
        


class student:
    ID
    device thing
    rating
    all_ratings[]
    def getrating():
        f = url.urlopen(ip_lol)
        rating = f.read()
        all_ratings.append(rating)

class device:
    ip_addr
    mac_addr
    

def setup_students():
    device dev_list[] #append a bunch of devices to this shit, yo!
    student student_list[]
    #iterate through a device list and get a list of student ids
    #create students
    return student_list
    
def get_ip_list():
    a_list[]
    a_list.append(IP_CONST)
    return a_list

def dev_connect(): #receive data from device
    #unpackage data
    #extract ip and  mac
    device thingy
    thingy.ip_addr = IP_CONST
    thingy.mac_addr = MAC_CONST
    return thingy
